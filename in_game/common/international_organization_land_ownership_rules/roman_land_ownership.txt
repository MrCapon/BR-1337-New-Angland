roman_land_ownership = {

	# applied to locations that are owned by the international organization
	modifier = {
	}
	
	# root = location, recipient = international organization
	can_add_trigger = {
		always = yes #
	}
	
	# root = location, recipient = international organization
	can_remove_trigger = {
		always = no  #
	}

	can_add_trigger = {
		always = yes
	}

	# root = location, recipient = international organization
	can_add_location_trigger = {
		OR = {
			region = region:balkan_region
			region = region:anatolia_region
		}
		is_land = yes
		is_passable = yes
	}
	
	# root = location, recipient = international organization
	on_added = {
	}
	
	# root = location, recipient = international organization
	on_removed = {
	}
	
	# root = location, recipient = international organization
	ai_desire_to_add = {
		value = 100
	}

	owned_location_color = rgb { 150 11 146 }
	
	allow_control_propagation = yes
}
